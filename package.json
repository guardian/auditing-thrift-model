{
	"name": "auditing-thrift-model",
	"description": "Thrift model for auditing updates",
	"version": "0.1.0",
	"scripts": {
		"compile": "thrift --gen js:node --out build src/main/thrift/auditing.thrift && node nodejs/package.js"
	},
	"main": "build/rewritten.js"
}